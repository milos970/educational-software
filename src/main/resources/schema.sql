CREATE TABLE IF NOT EXISTS person
(
    id      int PRIMARY KEY,
    nam VARCHAR(255),
    surname VARCHAR(255),
    username VARCHAR(255),
    email VARCHAR(255),
    password VARCHAR(255),
    authority VARCHAR(255)
);